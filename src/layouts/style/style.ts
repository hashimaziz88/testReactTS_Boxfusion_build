import { createStyles, css } from "antd-style";

export const useStyles = createStyles({
  div: css`
    display: flex;
    flex: 1;
    justify-content: center;
    align-items: center;
    height: 100dvh;
    width: 100dvh;
  `,
});
